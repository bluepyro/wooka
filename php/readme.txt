The contents inside this php folder are currently being publicly hosted in:

“http://oneglobalnet.com/wooka” and “http://oneglobalnet.com/wooka/client”

The code successfully authorizes through the Moves API OAuth server and also successfully requests data from the Moves API. Currently it is being displayed as the user profile data in a JSON string.